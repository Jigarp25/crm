import 'package:flutter/material.dart';

class LeadList extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('LeadList Screen', style: TextStyle(fontSize: 24)));
  }
}