import 'package:flutter/material.dart';

class DealList extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('DealList Screen', style: TextStyle(fontSize: 24)));
  }
}